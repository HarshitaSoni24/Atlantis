
import React from 'react';
import { Typography } from '@mui/material';

const MyLocations: React.FC = () => {
  return <Typography variant="h4">My Locations</Typography>;
};

export default MyLocations;
